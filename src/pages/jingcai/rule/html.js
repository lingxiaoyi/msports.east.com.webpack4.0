const content = require('./content.ejs')
const layout = require('layout')
const pageTitle = '竞猜规则'
const pageKeywords = ''
const pageDescription = ''
const canonical = ''
module.exports = layout({
    pageTitle,
    pageKeywords,
    pageDescription,
    canonical,
    content
})
